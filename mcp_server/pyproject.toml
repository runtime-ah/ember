[project]
name = "todo-mcp"
version = "0.1.0"
description = "MCP server exposing the task manager as Claude tools"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.2",
    "httpx>=0.27",
]

[tool.uv]
package = false
